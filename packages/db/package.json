{
  "name": "@insight-up/db",
  "version": "0.1.0",
  "description": "Database package with Prisma ORM for Insight Up Solutions",
  "main": "index.ts",
  "scripts": {
    "postinstall": "prisma generate",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "db:reset": "prisma migrate reset",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@prisma/client": "^5.19.1"
  },
  "devDependencies": {
    "prisma": "^5.19.1",
    "typescript": "^5.0.0",
    "vitest": "^3.2.4"
  },
  "types": "./index.ts"
}