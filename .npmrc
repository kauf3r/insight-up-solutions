# Force usage of npm instead of pnpm or yarn
package-manager-strict=true

# Workspace configuration for npm
prefer-workspace-packages=true

# Install peer dependencies automatically
auto-install-peers=true

# Save exact versions to avoid version drift
save-exact=true

# Disable package-lock generation in workspaces (except root)
package-lock=false